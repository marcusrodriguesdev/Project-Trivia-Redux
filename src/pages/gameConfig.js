import React from 'react';

class GameConfig extends React.Component {
  render() {
    return (
      <header>
        <h2 data-testid="settings-title">Game Configs</h2>
      </header>
    );
  }
}

export default GameConfig;
